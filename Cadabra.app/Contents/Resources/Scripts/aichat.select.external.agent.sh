#!/bin/sh
# aichat.select.external.agent.sh
# Entry point for the external ACP agent chooser.
# The ActionUI window is shown via ACTIONUI_WINDOW in Command.json.
# aichat.select.external.agent.init runs automatically as INIT_SUBCOMMAND_ID.
