#!/bin/sh
# aichat.mcp.inspect.sh
# Entry point for the MCP Servers inspector window.
# The ActionUI window is shown via ACTIONUI_WINDOW in Command.plist.
# aichat.mcp.inspect.init runs automatically as INIT_SUBCOMMAND_ID.
