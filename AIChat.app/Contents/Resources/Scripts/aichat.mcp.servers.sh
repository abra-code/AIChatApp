#!/bin/sh
# aichat.mcp.servers.sh
# Entry point for the MCP servers settings dialog.
# The ActionUI window is shown via ACTIONUI_WINDOW in Command.plist.
# aichat.mcp.servers.init runs automatically as INIT_SUBCOMMAND_ID.
