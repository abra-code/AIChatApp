#!/bin/sh
# aichat.select.local.model.sh
# Entry point for the local model selector.
# The ActionUI window is shown via ACTIONUI_WINDOW in Command.plist.
# aichat.select.local.model.init runs automatically as INIT_SUBCOMMAND_ID.
